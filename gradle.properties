org.gradle.jvmargs=-Xmx1024m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8

org.gradle.workers.max=1
org.gradle.parallel=false

# Kotlin daemon config (keep modest). If you still see daemon memory errors,
# you can flip this to 'false' to disable the daemon entirely.
kotlin.daemon.jvm.options=-Xmx768m,-XX:-UseParallelGC

